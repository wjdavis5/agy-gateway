{
  "name": "agy-gateway",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Zero-dependency HTTP gateway that forwards prompts to the headless agy CLI",
  "scripts": {
    "test": "node --test src/"
  },
  "dependencies": {}
}
